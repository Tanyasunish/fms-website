# Development-only fallback for the WhatsApp enquiry number (without + or special characters).
# In production the number is set via public/config.js (window.APP_CONFIG.enquiryPhoneNumber).
VITE_ENQUIRY_PHONE_NUMBER=your_phone_number_here